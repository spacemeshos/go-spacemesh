# Generate latest build and push it to dockerhub on push to develop branch.
# NOTE: This workflow does not include any tests, nor any dependencies, since bors guarantees
# that only code that passes all tests is ever pushed to develop.
name: push to dockerhub
env:
  DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
  DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}

on:
  push:
    branches:
      - '*'
    tags:
      - '*'

jobs:

  dockerpush:
    runs-on: ubuntu-latest
    steps:
    - name: Systemtest result
      run: |
        echo "::set-output name=mix-test::hello worlld\n"
      id: tests_logs
    - name: Tests Passed or Failed
      run: |
        LOGS='${{ steps.tests_logs.mix-test }}'
        ERROR='ERROR'
        if [[ "$LOGS" == *"$ERROR"* ]]; then
          exit 1
        fi
    # - name: checkout
    #   uses: actions/checkout@v2
    # - name: push to dockerhub
    #   run: make dockerpush
