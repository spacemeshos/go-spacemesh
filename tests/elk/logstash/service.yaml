apiVersion: v1
kind: Service
metadata:
  name: logstash
  labels:
    app: logstash
    chart: logstash-2.4.1
    heritage: Helm
  annotations:
spec:
  type: ClusterIP
  ports:
    - name: beats
      port: 5044
      protocol: TCP
      targetPort: beats
  selector:
    app: logstash
