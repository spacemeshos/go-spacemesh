apiVersion: v1
kind: Pod
metadata:
  name: curl
  labels:
    name: curl
spec:
  containers:
  - name: curl
    image: byrnedo/alpine-curl
    command:
      - "sh"
      - "-c"
      - >
        while true; do
          sleep 3600;
        done

